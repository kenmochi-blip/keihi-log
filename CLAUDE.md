# 経費ログ プロジェクトメモ

## LP 重要ポイント

### 価格プラン
- **ソロプラン**: 月額330円（税込）
- **チームプラン**: 月額825円（税込）
- ⚠️ **年額プランは現在提供していない**（LP・特商法表記から記載を削除済み）。
  サーバー側は `interval` で年額も扱えるまま（過去の年額契約が存在するため）。
  再開する場合は特商法表記（販売価格・支払時期・解約）とLPの復元が必要。
- 2週間無料トライアルあり（**クレカ登録不要**）
  - トライアル終了後は自動課金されない。ユーザーが自発的に有料プランへ切り替える必要がある。
  - 切替フロー: アプリ設定画面の「有料プランへ切り替える」ボタン → `upgradeLinks`（トライアルなし即時課金）へ遷移
  - `client_reference_id` に既存ライセンスキーを付与 → webhook が同じキーを延長（新キー発行なし）

### BYOKの説明（AI解析のキー）
- AI領収書解析はBYOK（Bring Your Own Key）方式
- お客様自身のGoogle AI Studio APIキーを設定して使用
- **使用モデル**: `gemini-3.1-flash-lite`（無料枠 500リクエスト/日・2026-06にCloud Consoleで実測確認）
  - ⚠️ 旧 `gemini-2.5-flash` は2025年12月の無料枠削減で 20 RPD になったため乗り換えた
  - 無料枠はGoogle都合で変動するため、LP等の数値を更新する際はCloud Console
    （Generative Language API →割り当て→「Request limit per model per day ... free tier」）で要確認
- ユーザー側の有料化は求めない。無料アカウントのままなら超過しても課金されない
  （その日の解析が止まるだけ）
- ⚠️ **LPの訴求は2026-09に変更済み**。「BYOKだから安い」ではなく
  **「データを預からないから、この価格で続けられる」**の一点に集約した（`public/index.html` の
  `#byok` セクション）。BYOKは柱ではなく脚注扱い。ナビの文言も「低価格の理由」→「価格の理由」。
- ⚠️ **BYOKは任意化する方針**（下記「AI解析キーの当方負担化」参照）

### AI解析キーの当方負担化（パターンB）— 2026-09 方針決定・実測待ちで実施

**結論：実測コストが想定内（売上比で誤差）なら実施する。** 標準を当方キーにし、BYOKは任意で残す。

#### なぜやるのか

- **オンボーディングの最大の障壁がキー取得**。「説明書いらずのシンプルさ」を謳いながら
  セットアップでAI Studioへの遷移とコピペを要求しており、setup.html にはトラブルシュート
  （「不審なリクエスト」エラー）まで置いてある＝ここが躓きポイントである自白。
  税理士が顧問先に勧める場面でも「まずキーを取ってください」と言わずに済む。
- **無料枠のままだとデータの扱いが弱い**（下記の規約参照）。有料枠に移すことは
  コスト負担と引き換えに**プライバシー訴求がむしろ強くなる**取引。
- BYOKは「安さの言い訳」から「自社完結したい大口向けの選択肢」へ格上げされる。

#### Gemini API 利用規約（2026-09 確認・決定的な差）

| | 無料枠（現行のBYOK運用） | 有料枠（当方負担） |
|---|---|---|
| プロンプト・レスポンスの利用 | **製品の提供・改善・開発に利用され得る** | **利用されない** |
| 人手によるレビュー | あり得る | なし |
| 処理の契約 | — | Googleがデータ処理者となるDPAの下 |
| ログ保持 | — | 禁止利用ポリシー違反検知・法令対応のみ／期間限定 |

規約は「機密・個人情報を無料サービスに送らないこと」を明示的に推奨しており、
**領収書＝財務書類はまさに該当**する。切替後は「お客様の領収書がAIの学習に使われることは
ありません」と明言できるようになる。
（EEA・スイス・英国は無料枠でも有料枠と同じ扱い。日本は対象外＝上表の通り。）
出典: https://ai.google.dev/gemini-api/terms

#### ⚠️ キーを増やしても影響範囲は縮まらない（検討済み・却下）

**Geminiのレート制限はプロジェクト単位で、APIキー単位ではない。**同一プロジェクト内の
キーは全て同じクォータを共有するため、ユーザーごとにキーを発行しても枯渇は全員に同時に起きる。
バックアップにも分散にもならない。
※ SheetsのSAプール（SA 1個=60回/分の per-user クォータがあるので分散できる）とは事情が異なる。
顧客ごとにプロジェクトを分けるのはクォータ回避として規約に抵触するリスクがあり、
1人運用では破綻するので採らない。
**影響範囲の最小化はBYOKを残すこと（＝パターンB）で達成する。** 当方キーが止まっても
顧客は自分のキーを入れれば即復旧できる。
出典: https://ai.google.dev/gemini-api/docs/rate-limits

#### 前提条件（実施前に必ず）

1. **実測**：`/api/data/aistats?secret=ADMIN_SECRET` で2週間。1枚あたりトークン数・
   テール分布・チーム別枚数を確認（下記「AI利用量の実測ツール」）
2. **GCPの有料化が必須**。無料枠500 RPDは**プロジェクト単位**なので当方キーに集約すると
   全顧客で1日500枚を奪い合い、月末集中を考えると数十チームで枯渇する。有料枠化で天井が外れる
3. **安全策を3段構えで先に設定**
   - Cloud Billing の予算アラート（例：月5,000円で通知）
   - Generative Language API の割り当て上限（暴走時の最終防波堤）
   - アプリ側のチーム別月次上限（下記実装②）
4. **デモ用と本番用でプロジェクト（キー）を分ける**。デモは公開・無認証で叩けるため、
   荒らされても契約者の解析を巻き込まない。用途が違うので規約上も問題なし。環境変数を2本に分けるだけ
5. **リポジトリの公開範囲を再検討する**（2026-09 に論点として認識）。
   `kenmochi-blip/keihi-log` は現在 **Public**。秘密情報は環境変数にあるのでリポジトリからは
   漏れないが、**悪用対策のパラメータが誰でも読める**状態にある。特に
   `api/gemini-proxy.js` のレート制限（IPあたり10分で20回）と、実装後は
   チーム別の月次上限のロジック。BYOKの現在は影響が限定的だが、
   **当方キー＋有料枠に切り替えると「回避方法が公開されている無料のAI」になる**。
   - 選択肢: ①リポジトリをPrivateにする ②制限値を環境変数に追い出してコードから消す
     ③現状維持（レート制限を厳しめにして許容する）
   - **判断は当方キー化の実行と同時に行う**こと。切替前に決めておかないと、
     公開状態のまま有料キーを晒すことになる

#### 実装内訳（コード2〜3日／文言1〜2日）

| # | 作業 | 工数 | 内容 |
|---|---|---|---|
| 1 | キー解決 | 30分 | `apiKey = 設定B5 \|\| process.env.GEMINI_API_KEY` を2箇所（Web版 `gemini()`・LINE版 `_lineGeminiKey()`）。**これだけで基本動作は完成** |
| 2 | 枠管理 | 半日 | 唯一の新規ロジック。`ai:quota:{sheetId}:{YYYY-MM}` を加算し超過で429+`quota_exceeded`。**当方キー使用時のみカウント**（BYOKは無制限）。プラン別上限は `_readPlanInfo` を流用 |
| 3 | クライアントのエラー処理 | 1-2h | `quota_exceeded` を1分岐追加→「今月の枠を使い切りました。ご自身のAPIキーを設定すると無制限です」 |
| 4 | セットアップStep3の任意化 | 半日 | UX上の本丸。現状は必須（`*`付き・検証ロジック実装済み）。任意化して「詳細設定」に畳む。5→4ステップ |
| 5 | 設定タブの格下げ | 2-3h | 「Gemini APIキー」→「自分のAPIキーを使う（任意）」＋今月の利用状況表示 |
| 6 | 直接呼び出し経路 | 30分 | 文言調整のみ（下記の制約参照） |
| 7 | 文言・配布物 | 1-2日 | index.html 24箇所／faq.html 24箇所（**3点同期**）／setup.html 31箇所／パンフ2種PDF再生成＋4面チェック／管理者ガイド3箇所／for-accountants 2箇所／プライバシーポリシーに再委託を明記。**診断（diagnosis.js）は変更不要** |

#### 環境変数

```
GEMINI_API_KEY        当方の本番用キー（有料枠プロジェクト）※未設定なら挙動は現行のまま
GEMINI_API_KEY_DEMO   デモ用キー（別プロジェクト推奨）
AI_PRICE_IN / AI_PRICE_OUT / AI_JPY_PER_USD  実測ダッシュボードの単価（既定 0.10 / 0.40 / 155）
```

**`GEMINI_API_KEY` を入れるまで挙動は完全に不変**＝フィーチャーフラグが実質タダで手に入る。
問題があれば環境変数を消すだけで即ロールバック（`LINE_ENABLED` と同じパターン）。

#### 既存ユーザーへの影響：ゼロ

B5にキーがある既存ユーザーは自動的にBYOKのまま動く。告知の第一文は必ず
**「ご利用中の設定はそのまま動きます。お手続きは不要です」**から始めること（不安にさせない）。
※ 既存ユーザーにも当方キーへ移る選択肢を与えるなら、設定タブに「経費ログのAIを使う（B5を空にする）」
操作が要る。無いと「なぜ自分だけ設定が必要なのか」という問い合わせになる。

#### 残る制約（許容する）

- **緊急フォールバック（`keihi_use_proxy='0'`）ではAI解析が使えない。** ブラウザから直接Geminiを
  叩く経路なので、当方の有料キーを渡すと開発者ツールで抜かれ、全顧客のクォータと請求が巻き添えになる。
  →「サーバー障害中はAI解析をご利用いただけません。手入力で登録してください」の案内を出す。
  BYOKを入れればこの場面でも即復活する。
- なおこのフォールバックはスコープが `drive.file` のみのため**実質は管理者専用の退避口**
  （メンバーはそもそもブラウザから直接シートを読めない）。実害は小さい。
- **BYOKユーザーへの注意喚起が必要**：無料枠のままだと学習に使われ得るため、
  「機密性を重視される場合はお客様側でも課金を有効にしてください」の一文を設定画面に置く。
  直感と逆転するので明示しないと誤解される。

#### 発信の順序（重要）

1. 実測（2週間）→ 単価と枠の枚数を確定
2. 有料化＋3段構えの上限設定
3. サーバー実装（1+2）を先行投入 ← 環境変数未設定なら挙動不変で安全
4. 環境変数を入れて自分＋協力的な数チームで試す
5. **セットアップUI（4）と文言（7）は一斉に**変更 ← ここから実質不可逆。
   LPだけ先に変えるとFAQ・パンフと矛盾し「キーが要るのか要らないのか」で混乱する

### AI利用量の実測ツール（2026-09 実装済み）

`api/_aiUsage.js` が Geminiレスポンスの `usageMetadata` を日次集計する。Web版・LINE版・デモの
3経路すべてに計測を挿入済み。**集計値のみ保存**し、画像・プロンプト・解析結果は保存しない。
計測失敗が本処理を止めないよう全て握り潰す。TTL 90日。

- KV: `ai:usage:{YYYY-MM-DD}`（req/err/トークン/ソース別/1kトークン刻みの分布）
  ・`ai:team:{YYYY-MM-DD}`（チーム別枚数）
- ダッシュボード: **`GET /api/data/aistats?secret=ADMIN_SECRET`**（HTML）／`&format=json`／`&days=30`
  - 1枚あたり実コスト・1,000チーム時の月額試算・テール分布・チーム別月間換算を表示
  - 単価は環境変数 `AI_PRICE_IN`/`AI_PRICE_OUT`/`AI_JPY_PER_USD` で実価格に追随（コード変更不要）
- 関数12個制限のためキャッチオール（`api/data/[...path].js`）内に実装

### Geminiクォータアラート対応手順

GCPコンソールのクォータアラートポリシーでメール通知を設定済み。
アラートメールが届いたら以下の手順で対応する。

#### 1. 現状確認（5分）

```
Cloud Console → APIとサービス → Generative Language API
→ 割り当てとシステム上限 → フィルタ: "Request limit per model per day"
```

「free tier」行の各モデルの「値」列を確認し、現在のモデル（`gemini-3.1-flash-lite`）の
RPDと、より大きい値を持つモデルを探す。

#### 2. 乗り換え先の選定基準

- free tier RPDが**現在値より大きい**（最低でも100以上を目安）
- モデル名に `lite` または `flash` を含む軽量系（領収書OCRにはProは不要）
- `experimental` は本番には使わない（廃止リスクが高い）
- `tts`・`image`・`embedding` は用途が違うので対象外

#### 3. 変更箇所（3ファイル・計3行）

```
public/js/gemini.js          : const MODEL = '...'
api/data/[...path].js        : const MODEL = '...'  （handleGemini関数内）
api/gemini-proxy.js          : const MODEL = '...'  （デモ用）
```

#### 4. 文言の更新箇所

RPD値が変わった場合は以下を一括修正する（数値は実測値に合わせる）：

```
public/index.html   : 「無料枠は1日XXXリクエスト」（BYOK説明セクション・FAQ3）
public/faq.html     : 「1日XXX件」（q402・q402b の2箇所）
public/setup.html   : 「1日あたりXXXリクエストまで無料」
api/_faq-data.js    : 上記faqと同じ内容のプレーンテキスト版
CLAUDE.md           : 本セクションの「実測確認」の値と日付
```

#### 5. バージョンバンプ・コミット・プッシュ

```bash
# app.html のバージョンクエリを更新（キャッシュ無効化）
# 例: gemini.js?v=YYYYMMDD

git add -A
git commit -m "Geminiモデルを XXX に変更（無料枠YYY RPD・YYYY-MM確認）"
git branch -f main claude/rebuild-receipt-app-Ft3lE
git push origin main
git push origin claude/rebuild-receipt-app-Ft3lE
```

#### 6. 動作確認

デプロイ後、デモモードで領収書を1枚読み取って正常に解析されることを確認する。

## アーキテクチャメモ
- Vanilla JS SPA + Bootstrap 5
- Vercel ホスティング（静的サイト + Serverless Functions）
- Vercel KV（ライセンス管理・URLエイリアス管理）
- 開発ブランチ: `claude/rebuild-receipt-app-Ft3lE`

### データアクセス方式（B' へ移行中）
- **旧方式**: 各ユーザーのOAuthトークンで Google Sheets / Drive API を直接呼ぶ。
  メンバーは `spreadsheets` スコープの追加認可が必要で、共有シートに直接アクセスしていた。
- **B'（新方式・移行中）**: Vercel Functions がサービスアカウント（SA）でシートを代理読み書きする
  プロキシ。メンバーはシートへの直接アクセス権・追加スコープ不要。
  - SA: `keihi-log-proxy@keihi-log.iam.gserviceaccount.com`（鍵は Vercel 環境変数 `GOOGLE_SA_KEY`）
  - エンドポイントは単一のキャッチオール関数 `api/data/[...path].js` に集約
    （Vercel Hobby の「関数12個」上限内に収めるため。B'のリソースを増やしてもここに足す）
  - 認可: クライアントは Google ID トークンを `Authorization: Bearer` で送り、サーバーが
    対象シートの「マスタ表」を SA で読んでメンバー判定（admin=全件 / 一般=自分の行のみ）
  - クライアント側はオプトイン（`localStorage.keihi_use_proxy === '1'`）で段階移行中。
    失敗時は従来の直接アクセスにフォールバック。
- SA を使うには各チームのスプレッドシート（および証票フォルダ）に SA をエディタ共有する必要がある。

### Vercel プラン注意（2026-09 に Pro へ移行済み）
- **Pro プラン**。関数12個の制限は外れたが、既存コードは `api/data/[...path].js`（1.6万行）に
  集約されたまま。**大規模リファクタは行わない**方針（本番稼働中のため）。
  その領域を触るときに、ついでに切り出していく。
- `maxDuration` は 180 秒（Gemini呼び出しのクライアント側タイムアウト55秒に対する余裕を確保）。
  Hobby時代は60秒上限に対し55秒で余裕5秒しかなく、Geminiが遅いと関数ごと殺される状態だった。
- **Spend Management**: On-Demand Budget $20・通知ON・**Pause Projects は OFF**
  （ONにするとチーム内の全プロジェクトの本番が停止し、経費ログが落ちるため）。
- **Preview Deployment Protection（Vercel Authentication）を有効化**。
  対象は Preview のみ（**Standard Protection**。"All Deployments" にすると本番も保護され
  keihi-log.com が誰も見られなくなる）。
  - 理由: プレビューURLが漏れると `api/gemini-proxy`（無認証・IPレート制限のみ）を
    第三者に叩かれ、当方のGeminiキーと請求が巻き添えになる。
  - ⚠️ **LINEのWebhookをプレビューに向けた検証は、保護が有効だと弾かれる**
    （LINE側はVercelにログインできないため）。検証するときは
    Settings → Deployment Protection を一時的にOFF→検証→戻す。

### スケール設計メモ（2026-08 検討・1,000→1万チームを見据えた整理）

⚠️ **AI解析キーを当方負担化すると、この前提が1つ崩れる**（Geminiが顧客側の分散資源でなくなり、
希少資源が7つ目として増える。爆発半径も拡大）。緩和策はBYOKを任意で残すこと＝パターンB。
詳細は上記「AI解析キーの当方負担化」を参照。

**前提**: 重い資源ほど顧客側に分散しており（Gemini=BYOK / 証票=顧客Drive / LINE=Reply縛りで無料）、
全チームで奪い合う希少資源は次の6つだけ。障壁はすべてここから生まれる。
①Sheets APIクォータ（1プロジェクト300回/分・SA 1個60回/分） ②SAが1個（全シートの編集者=爆発半径も最大）
③KV ④LINE公式アカウント1個 ⑤`GOOGLE_SA_KEY` 1本（SA認証・URL署名・トークン暗号化を兼務） ⑥運用者1人

**〜1,000チーム（金＋数日の工事で可。設計変更不要）**
- ピーク見積もり: 1,000チームで約40〜60リクエスト/分 → プロジェクト上限300には収まるが **SA 1個の60/分に当たる**
- [ ] SAプール配線: セットアップ時に担当チーム数最少のSAを選んで共有し、KVに `sa:{sheetId}` を記録。
      記録なし=SA#1 のデフォルトにすれば既存チームは無移行。SAの実体追加は必要になってから（随時足す方式でよい）。
      ※SAは各シートの editor なので、後からでも SA#1 が新SAを既存シートに共有追加するバッチ移行が可能
- [ ] Vercel Pro / KV有料化（売上比で誤差。タイミングだけの問題）
- [ ] `_buildNameMap()`（api/data/[...path].js）の全件KVスキャンを逆引きキーに置き換え（会計事務所ユーザー増で先に詰まる）
- [ ] LINE証票トークン失効（invalid_grant）の管理者向け通知（自己修復は実装済・気づく仕組みが未実装）
- [ ] OAuthアプリの公開ステータス確認（「テスト中」だと100ユーザーで停止）
- [ ] 手動運用の自動化（ライセンス発行・200チーム到達カウント等）

**〜1万チーム（設計判断が3つ）**
1. **クォータ増枠がGoogleに認められ続けるかが生命線**（実プロダクトなら数千/分の前例あり・保証はない）。
   複数プロジェクトでの上限掛け算は API 利用規約のクォータ回避禁止に抵触するため正攻法でない。
2. **シート全件読み（A2:U）は経過年数で壊れる**: 数万行チームはKVキャッシュ書込が静かに失敗→毎回Sheets直読み
   →そのチームだけでクォータを大食い。対処は**年度ごとのシート分割（年度アーカイブ）**。思想を曲げずに済む。
   大口チームが3年目に入る前に着手。
3. **秘密鍵の爆発半径**: `GOOGLE_SA_KEY` 漏洩=全顧客のシート読み書き+署名+復号が同時陥落。
   鍵ローテーション手順・SAプールごとの鍵分離・監査ログの体制設計が必要（機能でなく体制の話）。
- その他: LINEアカウント凍結=全顧客同時停止（大口向けBYOチャンネル検討）／監査ロジック二重管理の1本化／1人運用の限界

**Sheetsクォータ対応手順（Geminiクォータと同じ運用）**
1. 実測: Cloud Console → APIとサービス → Google Sheets API → 割り当て →「Read requests per minute per user」のグラフ
2. GCPプロジェクトに請求先アカウントを紐付けておく（増枠申請の土俵。Sheets API自体は無料のまま）
3. **トリガー: 実測ピークが上限の50%（SA単体なら30/分）に達したら増枠申請**（実績グラフを添付）。
   増枠は実績で審査されるため、実績がないうちの申請は通らない/最小限で終わる。数日〜数週間で下りるので先取り不要。
4. 100チーム前後でSAプール配線を投入

**総括**: 金で解決できるものは全部安い（1万チーム売上比でインフラ1%未満）。真の制約は
①Googleの増枠（他社依存） ②時間経過で肥大するシート ③1人運用 の3つ。①②は先手が打てる。
最終的に「シート=唯一の原本」→「DBが正・シートは常時同期ミラー」のハイブリッド化判断が来る可能性はあるが、
「顧客が自分のDriveでデータを持てる」という思想はミラー方式でも守れる。

## Git 運用ルール（重要）
- 本番URL（keihi-log.com）は `main` ブランチからデプロイされる
- **開発は `claude/rebuild-receipt-app-Ft3lE` で行い、デプロイ可能になったら `main` を
  そのコミットへ早送り（fast-forward）して揃える**。
  - **原則：フィーチャーブランチへのコミット・プッシュと同時に常に main も更新する。**
    「まだ本番にしないで」と明示された場合のみ main 更新を保留する。
  - 手順：①フィーチャーブランチにコミット＆プッシュ
         → ②`git branch -f main claude/rebuild-receipt-app-Ft3lE && git push origin main`
  - ❌ cherry-pick での個別同期は**禁止**。過去にこれで平行二重履歴と
    依存ファイル取りこぼし（`_sa.js` 欠落で本番ビルド失敗）が発生したため。
  - 大きな機能の移行中で main を一時的に揃えられない場合は、その旨をユーザーに伝えて判断を仰ぐ。
- リセット/force-push 前は必ず「main にしか無い実体変更が無いか」を `git diff` で確認すること。

## やることリスト

### 🟢 OAuth スコープ審査について

- **審査不要**（確認済み）
  - 現在のスコープ（`drive.file` / `userinfo.email` / `openid` / `email` / `profile`）はすべてGCPコンソールで「非機密」に分類されており、Googleの検証作業は不要
- [ ] **OAuthサポートメールを変更**（任意・低優先度）
  - GCPコンソール → OAuth同意画面 → サポートメールを個人メール（kenmochi@smartandsmooth.com）から変更

### 🟢 B'（SAプロキシ）実装の進捗

- [x] B'サーバー基盤（`_sa.js` / `_verifyToken.js` / `api/data/[...path].js` ルーター）
- [x] `/api/data/health` 疎通確認（SA認証OK・共有シート読み取りOK）
- [x] `GET /api/data/expenses`（SA読み取り + マスタ表によるメンバー認可 + 60秒KVキャッシュ）
- [x] クライアント `readExpenses` をプロキシ経由に配線（オプトイン・フォールバック付き）
- [x] 残りの読み取り（masters / settings）をプロキシ化
- [x] 経費の書き込み（append / edit / delete / approve / settle / unsettle）をプロキシ化
  - 精算済（実精算）は編集・削除不可（電帳法）。誤精算の訂正用に admin 限定の unsettle を用意
- [x] 設定シート書き込み（B2-B7）をプロキシ化（`PUT /api/data/settings`・admin専用・セルホワイトリスト）
  - セットアップ直後（SA共有前）の書き込みは作成者トークンで直接 update のまま
- [x] 領収書アップロードを SA 経由化（`POST /api/data/receipt`・設定B4のフォルダへSA保存）
  - 閲覧は読み取り時にサーバーが Drive URL → HMAC署名付きプロキシURL（`GET /api/data/receipt`・24hTTL・
    `immutable`キャッシュ）へ書き換え。署名は閲覧権のある経費にのみ発行。
  - 書き込み時はサーバーが署名URL→永続Drive URLへ逆変換（`_normalizeImageLinks`）し正準URLを保存。
  - ⚠️ 前提: 各チームの**証票フォルダにも SA をエディタ共有**が必要（共有前は upload/閲覧が失敗）。
    署名URLの秘密鍵は `GOOGLE_SA_KEY` から導出（新規環境変数なし）。
- [x] Gemini をサーバープロキシ化（`POST /api/data/gemini`・設定B5をSAで読み代理呼び出し）
  - APIキーはブラウザに一切返さない。Geminiの生JSONのみ透過。プロキシOFF時は従来の直接呼び出し。
- [x] メンバーのシートアクセスをプロキシ完全移行（デフォルトON化）
  - `keihi_use_proxy !== '0'` でデフォルトON（緊急時は '0' でOFF）。
- 運用中の本番シート（2チーム）には SA をエディタ共有済み

### 🔧 技術的負債

- [x] **マスタ管理タブ（admin.js）を廃止して設定タブに一本化** → 完了
  - admin.js は settings.js の完全上位互換だったため削除。nav ボタンも元々なし。

- [x] **`js/picker.js` を削除** → 完了（コミット 4e11c56）

### 📱 LINE連携（チームプラン限定・**本番稼働中** 2026-07-05〜）

設計: `docs/line-integration-design.md`。LINE公式アカウントに領収書画像を送ると送信者の
経費ログに登録される。**全応答は Reply API のみ**（Push等は使わない＝無料枠を消費しない）。
本番bot `@705rxpww`・Webhook `https://keihi-log.com/api/data/linewebhook`・`LINE_ENABLED=1`。

- サーバー実装は全て `api/data/[...path].js` 内（関数12個制限のため新規ファイルなし）
  - `POST /api/data/linewebhook`（署名検証・生ボディ必須）／`/linecode`（admin）／`/lineunlink`（admin）
  - ⚠️ **`bodyParser` を無効化**（`config.api.bodyParser=false`）。LINE署名検証に生ボディが要るため。
    全ボディは `_readRaw`→`_body` で手動パース（`req._rawBody` に1度だけキャッシュ）。
- 環境変数（プレビュー用/本番用でチャンネルを分ける）:
  `LINE_CHANNEL_SECRET`・`LINE_CHANNEL_ACCESS_TOKEN`・`LINE_ENABLED`（"1"でON＝キルスイッチ）・
  `LINE_ADD_FRIEND_URL`（公式アカウント友だち追加URL。連携コードモーダルにQR表示。未設定でも可）
- 識別: メールありメンバーは P列=本人メール。メールなし（LINE専用）は合成ID `line:{sha256}先頭12`。
  設定タブの「LINE専用メンバー」ボタンで合成IDメンバーをマスタ表に追加＋コード発行。
- 複数経費ログ併用（実装済み）: 1つのLINEアカウントで複数の経費ログに連携可（連携コードを追加入力で追記）。
  複数連携中は画像送信時に登録先の組織を選ぶ（postback `pickorg`）。確認pendingに `sheetId` を保持し
  修正・登録・再監査は選択済みログを対象（`_pendingLink`）。履歴・未精算は全ログ横断集計。詳細は設計書§12。
- ⚠️ **証票画像の保存はオーナーのOAuthトークン方式**（`POST/GET/DELETE /api/data/linedrivetoken`）。
  SAは容量が無くMy Driveに新規ファイルを作れないため、**オーナー（購入メール＝ログインメール）が
  設定タブで一度「証票保存を有効化」**し、リフレッシュトークンをKV（`line:drivetoken:{sheetId}`・
  AES-256-GCM暗号化・鍵はGOOGLE_SA_KEY派生）に保管。LINEサーバーはそれでアップロード→SAへ閲覧権付与
  （Web版 uploadFile と同じ）。未有効化時は証票なしで登録継続（ベストエフォート）。新規環境変数なし。
- ⚠️ **監査ロジックは二重管理**：クライアント `submit.js _runAuditChecks` と
  サーバー `api/data/[...path].js _serverAuditChecks` が同一ルール。**片方を変えたら必ず両方直す**。
  （将来Web側もサーバー監査を呼ぶ1本化が次の課題）
- 検証手順: Vercelプレビューに開発用botのWebhookを向け、実機LINEから画像送信→テスト用シートへ登録を確認。

### 📋 今後のやること

- [ ] **AI解析キーの当方負担化（パターンB）**（方針決定済・実測待ち／2026-09）
  - 手順・条件・実装内訳・環境変数はすべて上記「AI解析キーの当方負担化」に記載
  - **次のアクション**: `/api/data/aistats?secret=...` を2週間観測 → 1枚あたり単価を確定
  - 判断基準: 実測コストが売上比で誤差なら実施。想定外に高ければ枠の設計を見直す
  - **2026-09-04 の初期実測**: 1枚あたり 1,989トークン ＝ **0.036円**（試算0.05〜0.1円より安い）。
    1,000チーム × 100枚/月 なら月3,600円・売上比0.44%＝誤差。
    残る確認はテール（極端に重い1枚）とチーム別の分布
  - ⚠️ **切替と同時にリポジトリの公開範囲を決めること**（上記 前提条件5）

- [ ] **ブログ累計PVを `/licenses` に直接表示**（GA4 Data API をサーバー側から呼んで数値を取得・表示）
- [ ] **LINE連携をプレビュー環境でE2E検証**（開発用チャンネル作成・Webhook設定・実送信）→ 合格後 `LINE_ENABLED=1` で本番化
- [ ] **将来**: Web側の監査も `_serverAuditChecks` を呼ぶ形に1本化（二重管理の解消）

- [ ] **価格表示に「初期200チーム限定」を入れる**（方針は決定・実施は保留中／2026-08）
  - 値上げの余地を残しつつ「今申し込む理由」を作るための表記。**まだ実装していない。**
  - 決めた内容:
    - **チームプランのみ**が対象。上限は **200チーム**（＝チームプランのライセンス1件を1チームと数える）。
      **有料契約のみカウントし、トライアル中は含めない**（未転換のトライアルで枠が埋まるのを避ける）。
    - **ソロプラン（330円）は価格改定の対象外**。ただし「ずっと」と断言すると将来縛られるため
      表現の強さは要判断（Gemini無料枠の縮小等で前提が変わる可能性がある）。
    - **進捗（残り◯チーム・現在◯チーム）は絶対に表示しない。** 出すと「売れていない」ように見える。
      上限のみ宣言し、達成したら「改定しました」に文言を差し替える（＝撤収作業が発生しない）。
    - **「ご契約中は据え置き」を必ずセットで書く。** 無いと単なる煽りになり、到達後の申込者が損をした
      気分になる。据え置きを明記すると訴求が「安く買える」→「安い価格で固定できる」に変わる。
  - 文案:
    > チームプラン 月額825円（税込）／メンバー数に関わらず定額
    > **初期200チーム限定の価格です。** 200チームに達し次第、新規のお申し込み価格を改定します。
    > すでにご契約中の方は、その後も825円のまま据え置きます。
    > ソロプラン 月額330円（税込）／価格改定の対象外です。
  - 反映先: `public/index.html`（料金セクション 1525行付近）／`public/for-accountants.html`／
    `public/tokusho.html`（販売価格欄に「申込時点の表示価格・契約中は据え置き」）／
    `public/terms.html`（価格改定条項）／`public/faq.html`（「200チームに達したら値上がりしますか？」）
  - 運用メモ: **Stripeは既存サブスクの価格を自動では変えない。** 値上げは新しい Payment Link を作って
    `config.js` の `signupLinks` / `upgradeLinks` を差し替えるだけでよく、既存契約者は旧価格のまま残る。
    据え置きの約束を守るための追加実装は不要。200チーム到達の判定も `/licenses` の発行一覧で手動確認する
    （自動切替は事故のもとなので実装しない）。
  - 積み残しの論点: 値上げ後、ソロ→チームの差が2.5倍→5倍になりアップグレード障壁が上がる。
    「期間中のソロ→チーム切替も据え置き」は Stripe の価格出し分けが必要で複雑なため、
    当面は書かず、相談が来たら旧価格リンクを個別に送る運用とする。
